{
  "name": "chainlink-rpc-reliability-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prototype for comparing RPC providers while reading a Chainlink Data Feed.",
  "scripts": {
    "monitor": "node src/monitor.mjs",
    "dashboard": "node src/server.mjs",
    "check": "node --check src/monitor.mjs && node --check src/rpc-monitor.mjs && node --check src/server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
